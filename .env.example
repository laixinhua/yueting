# 生产环境若未配置反向代理，可填写 EBNR 完整地址（需服务端解决 CORS）
# VITE_EBNR_BASE=https://your-proxy.example.com/api/ebnr

# 开发/预览默认走 Vite 代理：/api/ebnr -> https://ebnr.xiyang6666.top

# ── 网易云音乐「开放平台」官方 API（developer.music.163.com）──
# ⚠️ 密钥属于机密，切勿写进被 git 跟踪的 .env.capacitor！
#    真实值请放在 .env.capacitor.local（已被 .gitignore 的 *.local 忽略，不会提交）。
#    以下仅为变量说明，复制去掉注释并填入你的凭证：
# VITE_NCM_APPID=开放平台控制台分配的 AppID
# VITE_NCM_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----（PEM，用于 RSA_SHA256 签名）-----
# VITE_NCM_PUBKEY=-----BEGIN PUBLIC KEY-----（PEM，用于 bizContent RSA 加密）-----
# VITE_NCM_PACKAGE_NAME=com.yueting.music
# VITE_NCM_FINGERPRINT=安卓签名指纹（控制台绑定的 packageName + fingerprint）
# 未配置时 src/api/neteaseOfficial.ts 的 isOfficialConfigured=false，自动回退 EBNR，不影响现有播放。

